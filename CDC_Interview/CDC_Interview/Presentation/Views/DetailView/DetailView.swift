import SwiftUI

struct DetailView: View {
  
    var body: some View {
        VStack(alignment: .leading, spacing: 16) {
            Text("detail view")
                .font(.largeTitle)
                .fontWeight(.bold)
                .padding(.top)
            
            
        }
    }
}

